{
  "name": "peluqueria-mama",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "dev:front": "parcel serve ./src/index.html",
    "build": "parcel build ./src/index.html --dist-dir build --public-url /peluqueria-home",
    "deploy": "gh-pages -d build",
    "build-and-deploy": "npm run build && npm run deploy"
  },
  "dependencies": {
    "@types/node": "^18.11.9",
    "@vaadin/router": "^1.7.4",
    "gh-pages": "^4.0.0",
    "parcel": "^2.7.0"
  }
}
